/* vim: set syntax=cpp: */
#pragma once
#include <string>
#include <maolan/config>


class OSSConfig : public Config
{
  public:
    OSSConfig();

    int fd;
    int format;
    int samplerate;
    std::size_t frag;
    std::size_t fragSize;
    std::size_t count;
    std::string device;
};
